<div class="container product">

<p id="notice"><%= notice %></p>

<h1 class="text-ctr">Listing Products</h1>
  <div class="row">
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Image url</th>
            <th>Colour</th>
            <th colspan="3"></th>
          </tr>
        </thead>
      <tbody id="products">
        <% @products.each do |product| %>
          <tr>
            <td style="width: 15%"><%= product.name %></td>
            <td style="width: 35%"><%= product.description %></td>
            <td style="width: 25%"><img src= "<%= product.image_url %>"></td>
            <td style="width: 15%"><%= product.colour %></td>
             <td style="width: 6em"><%= link_to 'Show', product %></td>
            <td style="width: 6em"><%= link_to 'Edit', edit_product_path(product) %></td>
            <td style="width: 6em"><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
      </table>

      <br>
  </div>
      <%= link_to 'New Product', new_product_path %>


